make a folder for the storage files

make the aliases for these functions be its own file

rename the functions to be more understanding

make a bash script that will download all these commands on to the system

make the script try to interpret weather its a zhs shell or a bash shell

if a zsh shell make it so that bash is an alias for zsh and more more...
